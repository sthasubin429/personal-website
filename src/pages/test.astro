<!doctype html>
<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Float Down Animation</title>
    <script src='https://cdn.tailwindcss.com'></script>
    <style>
      @keyframes floatDown {
        from {
          opacity: 0;
          transform: translateY(-30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .float-down {
        animation: floatDown 0.3s ease-out forwards;
      }

      .float-down-delay-1 {
        animation: floatDown 0.3s ease-out 0.2s forwards;
        opacity: 0;
      }

      .float-down-delay-2 {
        animation: floatDown 0.3s ease-out 0.4s forwards;
        opacity: 0;
      }

      .float-down-delay-3 {
        animation: floatDown 0.3s ease-out 0.6s forwards;
        opacity: 0;
      }
    </style>
  </head>
  <body class='bg-gray-100 min-h-screen flex items-center justify-center'>
    <div class='text-center space-y-8'>
      <!-- Main heading with immediate animation -->
      <h1 class='text-4xl font-bold text-gray-800 float-down'>Welcome!</h1>

      <!-- Subtitle with delay -->
      <p class='text-lg text-gray-600 float-down-delay-1'>
        This content floats down smoothly on page load
      </p>

      <!-- Card with longer delay -->
      <div class='bg-white p-8 rounded-lg shadow-lg max-w-md float-down-delay-2'>
        <h2 class='text-2xl font-semibold mb-4 text-gray-700'>Float Animation</h2>
        <p class='text-gray-600'>
          This card appears with a gentle float down effect using Tailwind CSS and custom keyframes.
        </p>
      </div>

      <!-- Button with longest delay -->
      <button
        class='bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg transition-colors float-down-delay-3'
      >
        Get Started
      </button>
    </div>
  </body>
</html>
